# Project Notes

- Update `GAME_VERSION` in `game.js` whenever the game changes.
- Keep the `style.css?v=` and `game.js?v=` query versions in `index.html` aligned with `GAME_VERSION`.
- After each requested change, run verification, then `git commit` and `git push`.
